#include "holberton.h"

/**
 * print_most_numbers - prints numbers from 0-9 except 2 and 4
 * Description: use _putchar
 *Return: 0-1, 3, 5-9
 */

void print_most_numbers(void)
{
	int num;

	if ((num =0 && num < 2) || (num = 3) || num =5 && num <= 9))
	{
		_putchar(num + '0');
	}
_putchar('\n')
